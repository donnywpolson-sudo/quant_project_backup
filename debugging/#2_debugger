Use the FULL combined code from my previous messages (all parts) as the input code.

OBJECTIVE
Strict intraday Globex 23/5 (18:00→16:00 America/New_York). Exclude 17:00–18:00 CME gap. No overnight holds. Zero leakage. Memory <16GB (target ≤14GB). seed=42. float32 only. Polars only (no pandas), pytz, chunked processing.

TASK
Fix the error completely while preserving behavior.

CONSTRAINTS
- Minimal, surgical changes only
- No refactor or logic changes
- Keep variables, flow, and structure unchanged
- Polars only (no pandas)
- Maintain float32 where used
- Preserve timezone correctness (America/New_York)

REQUIREMENTS
- Resolve root cause (no symptom fixes)
- No data leakage or causal violations introduced
- Maintain determinism (seed=42 unchanged)
- Ensure memory constraints are not exceeded

GUIDELINES
- Fix only what is strictly necessary
- Keep changes localized
- Prefer the simplest valid fix
- Add guards only if strictly required
- Do not rename variables or change structure unless required to fix the error

MULTI-FILE OUTPUT RULES
If code spans multiple files:
- Output EACH file separately
- Clearly label each file with its filename before the code block
- Each file must be independently copy-pasteable
- Preserve original file boundaries
- Do NOT merge files

CRITICAL
- Never merge modules into one file
- Do not omit any affected file
- Output FULL files, not diffs
- Do not ask clarification questions
- Do not defer work — complete the fix in this response

ERROR
<PASTE FULL TRACEBACK HERE>

OUTPUT FORMAT

File: <filename_1>
<full updated code>

File: <filename_2>
<full updated code>

(repeat as needed)

(no comments inside code)

FINAL SUMMARY (outside code):
Briefly describe the root cause and why the fix resolves it.